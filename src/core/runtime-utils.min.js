(function attachBibleTriviaCore(globalScope){function normalizeDifficultyMode(mode){if(typeof mode!=`string`)return`medium`;let lower=mode.trim().toLowerCase();return lower===`easy`?`easy`:lower===`medium`?`medium`:lower===`hard`?`hard`:lower===`super hard`||lower===`superhard`?`superHard`:`medium`}function normalizeRoundReviewPreference(value){if(typeof value!=`string`)return`ask`;let normalized=value.trim().toLowerCase();return normalized===`alwaysreview`||normalized===`always review`?`alwaysReview`:normalized===`alwaysskip`||normalized===`always skip`?`alwaysSkip`:`ask`}function isValidHighScoreRecord(record){return!!record&&typeof record==`object`&&[`score`,`timestamp`,`correct`,`total`].every(k=>k in record&&record[k]!==void 0)}function compareScoresDescending(a,b){let scoreDiff=Number(b.score)-Number(a.score);if(scoreDiff!==0)return scoreDiff;let timeA=Date.parse(a.timestamp),timeB=Date.parse(b.timestamp),safeA=Number.isNaN(timeA)?0:timeA;return(Number.isNaN(timeB)?0:timeB)-safeA}function getTopScoresByMode(scores,mode,limit){let safeLimit=Number.isFinite(limit)?Math.max(1,Math.round(limit)):5,normalizedMode=normalizeDifficultyMode(mode);return(Array.isArray(scores)?scores:[]).filter(isValidHighScoreRecord).map(record=>({...record,difficultyMode:normalizeDifficultyMode(record.difficultyMode)})).filter(record=>record.difficultyMode===normalizedMode).sort(compareScoresDescending).slice(0,safeLimit)}function normalizeAndTrimHighScores(scores,maxPerMode){let safeMax=Number.isFinite(maxPerMode)?Math.max(1,Math.round(maxPerMode)):5,modeKeys=[`easy`,`medium`,`hard`,`superHard`],sanitized=(Array.isArray(scores)?scores:[]).filter(isValidHighScoreRecord).map(record=>({...record,difficultyMode:normalizeDifficultyMode(record.difficultyMode)}));return modeKeys.flatMap(mode=>getTopScoresByMode(sanitized,mode,safeMax))}function getLocalDateKey(dateInput){let input=dateInput===void 0?/* @__PURE__ */ new Date:dateInput,date=input instanceof Date?input:new Date(input);return`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,`0`)}-${String(date.getDate()).padStart(2,`0`)}`}function getDateKeyOffset(dateKey,dayOffset){if(typeof dateKey!=`string`||!/^\d{4}-\d{2}-\d{2}$/.test(dateKey))return null;let[year,month,day]=dateKey.split(`-`).map(Number),date=new Date(year,month-1,day);return date.setDate(date.getDate()+dayOffset),getLocalDateKey(date)}function createDailySeed(dateKey,modeVersion){return`${dateKey}|${String(modeVersion||`v1`)}|daily`}function createSeededRandom(seedText){let hash=2166136261,text=String(seedText||`daily-seed`);for(let i=0;i<text.length;i++)hash^=text.charCodeAt(i),hash=Math.imul(hash,16777619);let state=hash>>>0;return()=>(state=Math.imul(state,1664525)+1013904223>>>0,state/4294967296)}function shuffleWithRandom(array,randomFn){let random=typeof randomFn==`function`?randomFn:Math.random,copy=Array.isArray(array)?[...array]:[];for(let i=copy.length-1;i>0;i--){let j=Math.floor(random()*(i+1));[copy[i],copy[j]]=[copy[j],copy[i]]}return copy}function getCappedStreak(streak,cap){let maxCap=Number.isFinite(cap)?Math.max(1,Math.round(cap)):10;return Math.min(Math.max(0,Math.round(streak||0)),maxCap)}function getStreakBonus(streak,bonusTable,cap){let table=bonusTable&&typeof bonusTable==`object`?bonusTable:{},capped=getCappedStreak(streak,cap);return capped<3?0:Number(table[capped]||0)}function getNextStreakBonusInfo(streak,bonusTable,cap){let table=bonusTable&&typeof bonusTable==`object`?bonusTable:{},maxCap=Number.isFinite(cap)?Math.max(1,Math.round(cap)):10,current=Math.max(0,Math.round(streak||0));if(current>=maxCap)return{label:`Cap reached at ${maxCap}`};let nextStreak=current+1,nextBonus=getStreakBonus(nextStreak,table,maxCap);return nextBonus<=0?{label:`Next bonus at 3`}:{label:`Next: +${nextBonus} at ${nextStreak}`}}function createZeroedModeMap(){return{easy:0,medium:0,hard:0,superHard:0}}function createDefaultPlayerStats(){return{key:`lifetime`,totalCorrectClicks:0,correctSecondsByMode:createZeroedModeMap(),correctCountByMode:createZeroedModeMap(),correctByCategory:{"Old Testament":0,"New Testament":0,General:0},currentStreak:0,allTimeBestStreak:0,lastGameMaxStreak:0}}function sanitizePlayerStats(rawStats,difficultyModeKeys){let modeKeys=Array.isArray(difficultyModeKeys)&&difficultyModeKeys.length?difficultyModeKeys:[`easy`,`medium`,`hard`,`superHard`],defaults=createDefaultPlayerStats(),raw=rawStats&&typeof rawStats==`object`?rawStats:{},sanitized={key:`lifetime`,totalCorrectClicks:Number.isFinite(raw.totalCorrectClicks)?Math.max(0,Math.round(raw.totalCorrectClicks)):0,correctSecondsByMode:createZeroedModeMap(),correctCountByMode:createZeroedModeMap(),correctByCategory:{"Old Testament":0,"New Testament":0,General:0},currentStreak:Number.isFinite(raw.currentStreak)?Math.max(0,Math.round(raw.currentStreak)):0,allTimeBestStreak:Number.isFinite(raw.allTimeBestStreak)?Math.max(0,Math.round(raw.allTimeBestStreak)):0,lastGameMaxStreak:Number.isFinite(raw.lastGameMaxStreak)?Math.max(0,Math.round(raw.lastGameMaxStreak)):0};return modeKeys.forEach(mode=>{let seconds=raw.correctSecondsByMode?.[mode],count=raw.correctCountByMode?.[mode];sanitized.correctSecondsByMode[mode]=Number.isFinite(seconds)?Math.max(0,Math.round(seconds)):defaults.correctSecondsByMode[mode],sanitized.correctCountByMode[mode]=Number.isFinite(count)?Math.max(0,Math.round(count)):defaults.correctCountByMode[mode]}),[`Old Testament`,`New Testament`,`General`].forEach(category=>{let value=raw.correctByCategory?.[category];sanitized.correctByCategory[category]=Number.isFinite(value)?Math.max(0,Math.round(value)):0}),sanitized.allTimeBestStreak<sanitized.currentStreak&&(sanitized.allTimeBestStreak=sanitized.currentStreak),sanitized}function createDefaultDailyStats(){return{key:`lifetime`,currentDailyStreak:0,bestDailyStreak:0,lastCompletedDateKey:null,totalDailyCompletions:0,totalDailyAttempts:0}}function sanitizeDailyStats(rawStats){let raw=rawStats&&typeof rawStats==`object`?rawStats:{},sanitized={key:`lifetime`,currentDailyStreak:Number.isFinite(raw.currentDailyStreak)?Math.max(0,Math.round(raw.currentDailyStreak)):0,bestDailyStreak:Number.isFinite(raw.bestDailyStreak)?Math.max(0,Math.round(raw.bestDailyStreak)):0,lastCompletedDateKey:typeof raw.lastCompletedDateKey==`string`&&raw.lastCompletedDateKey?raw.lastCompletedDateKey:null,totalDailyCompletions:Number.isFinite(raw.totalDailyCompletions)?Math.max(0,Math.round(raw.totalDailyCompletions)):0,totalDailyAttempts:Number.isFinite(raw.totalDailyAttempts)?Math.max(0,Math.round(raw.totalDailyAttempts)):0};return sanitized.bestDailyStreak<sanitized.currentDailyStreak&&(sanitized.bestDailyStreak=sanitized.currentDailyStreak),sanitized}function sanitizeDailyChallengeRecord(rawRecord,modeVersion){let raw=rawRecord&&typeof rawRecord==`object`?rawRecord:{},questionIds=Array.isArray(raw.questionIds)?raw.questionIds.filter(id=>Number.isFinite(id)).map(id=>Math.round(id)):[];return{dateKey:String(raw.dateKey||``).trim(),seed:String(raw.seed||``),questionIds,attempts:Number.isFinite(raw.attempts)?Math.max(0,Math.round(raw.attempts)):0,bestScore:Number.isFinite(raw.bestScore)?Math.max(0,Math.round(raw.bestScore)):0,lastScore:Number.isFinite(raw.lastScore)?Math.max(0,Math.round(raw.lastScore)):0,completed:!!raw.completed,completedAt:raw.completedAt?String(raw.completedAt):null,modeVersion:String(raw.modeVersion||modeVersion||`v1`)}}function sanitizeDailyChallengeMap(rawMap,modeVersion){let map=rawMap&&typeof rawMap==`object`?rawMap:{},sanitized={};return Object.keys(map).forEach(dateKey=>{let record=sanitizeDailyChallengeRecord(map[dateKey],modeVersion);record.dateKey&&(sanitized[record.dateKey]=record)}),sanitized}function isValidGameRecord(record,difficultyModeKeys,normalizeDifficultyModeFn){if(!record||typeof record!=`object`||![`timestamp`,`correct`,`total`,`score`,`percent`].every(k=>k in record&&record[k]!==void 0))return!1;let normalize=typeof normalizeDifficultyModeFn==`function`?normalizeDifficultyModeFn:normalizeDifficultyMode,modes=Array.isArray(difficultyModeKeys)&&difficultyModeKeys.length?difficultyModeKeys:[`easy`,`medium`,`hard`,`superHard`];return record.difficultyMode=normalize(record.difficultyMode),modes.includes(record.difficultyMode)}function isValidHighScore(record,difficultyModeKeys,normalizeDifficultyModeFn){if(!record||typeof record!=`object`||![`score`,`timestamp`,`correct`,`total`].every(k=>k in record&&record[k]!==void 0))return!1;let normalize=typeof normalizeDifficultyModeFn==`function`?normalizeDifficultyModeFn:normalizeDifficultyMode,modes=Array.isArray(difficultyModeKeys)&&difficultyModeKeys.length?difficultyModeKeys:[`easy`,`medium`,`hard`,`superHard`];return record.difficultyMode=normalize(record.difficultyMode),modes.includes(record.difficultyMode)}function isValidSetting(key,value,difficultyModeKeys,roundReviewPreferenceKeys,normalizeDifficultyModeFn,normalizeRoundReviewPreferenceFn){let modeKeys=Array.isArray(difficultyModeKeys)&&difficultyModeKeys.length?difficultyModeKeys:[`easy`,`medium`,`hard`,`superHard`],reviewKeys=Array.isArray(roundReviewPreferenceKeys)&&roundReviewPreferenceKeys.length?roundReviewPreferenceKeys:[`ask`,`alwaysReview`,`alwaysSkip`],normalizeMode=typeof normalizeDifficultyModeFn==`function`?normalizeDifficultyModeFn:normalizeDifficultyMode,normalizeReview=typeof normalizeRoundReviewPreferenceFn==`function`?normalizeRoundReviewPreferenceFn:normalizeRoundReviewPreference;return key===`difficultyMode`?modeKeys.includes(normalizeMode(value)):key===`roundReviewPreference`?reviewKeys.includes(normalizeReview(value)):!1}function isValidPlayerStatsRecord(record){return!(!record||typeof record!=`object`||record.key!==`lifetime`||typeof record.totalCorrectClicks!=`number`||typeof record.currentStreak!=`number`||typeof record.allTimeBestStreak!=`number`||typeof record.lastGameMaxStreak!=`number`||!record.correctSecondsByMode||typeof record.correctSecondsByMode!=`object`||!record.correctCountByMode||typeof record.correctCountByMode!=`object`||!record.correctByCategory||typeof record.correctByCategory!=`object`)}function isValidDailyChallengeRecord(record){return!(!record||typeof record!=`object`||typeof record.dateKey!=`string`||!record.dateKey.trim()||typeof record.seed!=`string`||!Array.isArray(record.questionIds)||typeof record.attempts!=`number`||typeof record.bestScore!=`number`||typeof record.lastScore!=`number`||typeof record.completed!=`boolean`||!(record.completedAt===null||typeof record.completedAt==`string`)||typeof record.modeVersion!=`string`)}function isValidDailyStatsRecord(record){return!(!record||typeof record!=`object`||record.key!==`lifetime`||typeof record.currentDailyStreak!=`number`||typeof record.bestDailyStreak!=`number`||!(record.lastCompletedDateKey===null||typeof record.lastCompletedDateKey==`string`)||typeof record.totalDailyCompletions!=`number`||typeof record.totalDailyAttempts!=`number`)}globalScope.BibleTriviaCore=Object.assign({},globalScope.BibleTriviaCore,{normalizeDifficultyMode,normalizeRoundReviewPreference,isValidHighScoreRecord,compareScoresDescending,getTopScoresByMode,normalizeAndTrimHighScores,getLocalDateKey,getDateKeyOffset,createDailySeed,createSeededRandom,shuffleWithRandom,getCappedStreak,getStreakBonus,getNextStreakBonusInfo,createZeroedModeMap,createDefaultPlayerStats,sanitizePlayerStats,createDefaultDailyStats,sanitizeDailyStats,sanitizeDailyChallengeRecord,sanitizeDailyChallengeMap,isValidGameRecord,isValidHighScore,isValidSetting,isValidPlayerStatsRecord,isValidDailyChallengeRecord,isValidDailyStatsRecord})})(window);