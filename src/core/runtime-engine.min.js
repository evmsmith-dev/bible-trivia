(function attachBibleTriviaEngine(globalScope){function getTimeLimitForMode(modeTiming,activeMode,fallbackSeconds){let timing=modeTiming&&typeof modeTiming==`object`?modeTiming:{},fallback=Number.isFinite(fallbackSeconds)?Math.max(1,Math.round(fallbackSeconds)):25,value=timing[activeMode];return Number.isFinite(value)?Math.max(1,Math.round(value)):fallback}function calculatePointBreakdown(input){let payload=input&&typeof input==`object`?input:{},basePoints=Number.isFinite(payload.basePoints)?Math.max(0,payload.basePoints):0,dailyBonusPoints=Number.isFinite(payload.dailyBonusPoints)?Math.max(0,payload.dailyBonusPoints):0,timeLeftAtAnswer=Number.isFinite(payload.timeLeftAtAnswer)?Math.max(0,payload.timeLeftAtAnswer):0,timeLimit=Number.isFinite(payload.timeLimit)?Math.max(1,payload.timeLimit):25,streakBonus=Number.isFinite(payload.streakBonus)?Math.max(0,payload.streakBonus):0,maxBonus=basePoints,speedBonus=Math.max(0,maxBonus*((timeLeftAtAnswer+.75)/timeLimit)),roundedSpeedBonus=Math.round(speedBonus),roundedStreakBonus=Math.round(streakBonus);return{base:Math.round(basePoints),dailyBaseBonus:Math.round(dailyBonusPoints),speedBonus:roundedSpeedBonus,streakBonus:roundedStreakBonus,total:Math.round(basePoints+dailyBonusPoints+roundedSpeedBonus+roundedStreakBonus)}}function selectBalancedQuestionsForMode(input){let payload=input&&typeof input==`object`?input:{},modeKey=payload.modeKey,randomFn=typeof payload.randomFn==`function`?payload.randomFn:Math.random,questionCount=Number.isFinite(payload.questionCount)?Math.max(1,Math.round(payload.questionCount)):10,allQuestions=Array.isArray(payload.allQuestions)?payload.allQuestions:[],modeMap=payload.modeMap&&typeof payload.modeMap==`object`?payload.modeMap:{},categories=Array.isArray(payload.categories)&&payload.categories.length?payload.categories:[`Old Testament`,`New Testament`,`General`],modeConfig=modeMap[(typeof payload.normalizeDifficultyMode==`function`?payload.normalizeDifficultyMode:(value=>String(value||`medium`)))(modeKey)]||modeMap.medium;if(!modeConfig||!Array.isArray(modeConfig.difficulties))return[];let difficulties=modeConfig.difficulties,catRemaining={"Old Testament":4,"New Testament":4,General:2},diffRemaining=Object.assign({},modeConfig.difficultyTargets||{}),matrix=modeConfig.matrix||{},available=allQuestions.filter(question=>difficulties.includes(question.difficulty)),pools={};difficulties.forEach(difficulty=>{categories.forEach(category=>{pools[`${difficulty}|${category}`]=[]})}),available.forEach(question=>{let key=`${question.difficulty}|${question.category}`;pools[key]&&pools[key].push(question)}),Object.keys(pools).forEach(key=>{let list=pools[key];for(let i=list.length-1;i>0;i--){let j=Math.floor(randomFn()*(i+1));[list[i],list[j]]=[list[j],list[i]]}});let selectedIds=/* @__PURE__ */ new Set,shortages=[];difficulties.forEach(difficulty=>{categories.forEach(category=>{let needed=matrix[difficulty]?.[category]??0;if(needed===0)return;let pool=pools[`${difficulty}|${category}`]||[],take=Math.min(needed,pool.length);for(let i=0;i<take;i++){let question=pool[i];selectedIds.add(question.id),catRemaining[category]=(catRemaining[category]||0)-1,diffRemaining[difficulty]=(diffRemaining[difficulty]||0)-1}take<needed&&shortages.push({missing:needed-take,difficulty,category})})});let remaining=available.filter(question=>!selectedIds.has(question.id));for(let i=remaining.length-1;i>0;i--){let j=Math.floor(randomFn()*(i+1));[remaining[i],remaining[j]]=[remaining[j],remaining[i]]}shortages.forEach(shortage=>{let needed=shortage.missing;for(;needed>0;){let candidates=remaining.filter(question=>(catRemaining[question.category]||0)>0&&(diffRemaining[question.difficulty]||0)>0);if(candidates.length===0)break;candidates.sort((a,b)=>{let categoryDelta=(catRemaining[b.category]||0)-(catRemaining[a.category]||0);return categoryDelta===0?(diffRemaining[b.difficulty]||0)-(diffRemaining[a.difficulty]||0):categoryDelta});let chosen=candidates[0];selectedIds.add(chosen.id),catRemaining[chosen.category]=(catRemaining[chosen.category]||0)-1,diffRemaining[chosen.difficulty]=(diffRemaining[chosen.difficulty]||0)-1,--needed,remaining=remaining.filter(question=>question.id!==chosen.id)}});let finalSelection=available.filter(question=>selectedIds.has(question.id));for(let i=finalSelection.length-1;i>0;i--){let j=Math.floor(randomFn()*(i+1));[finalSelection[i],finalSelection[j]]=[finalSelection[j],finalSelection[i]]}if(finalSelection.length<questionCount){let remainingPool=available.filter(question=>!selectedIds.has(question.id));for(let i=remainingPool.length-1;i>0;i--){let j=Math.floor(randomFn()*(i+1));[remainingPool[i],remainingPool[j]]=[remainingPool[j],remainingPool[i]]}remainingPool.forEach(question=>{finalSelection.length>=questionCount||selectedIds.has(question.id)||(selectedIds.add(question.id),finalSelection.push(question))})}return finalSelection.slice(0,questionCount)}function startCountdownThenGame(input){let payload=input&&typeof input==`object`?input:{},overlayEl=payload.overlayEl||null,numberEl=payload.numberEl||null,mode=payload.mode,normalMode=payload.normalMode,dailyMode=payload.dailyMode,preserveBonusStreak=payload.preserveBonusStreak===!0,startDailyChallenge=typeof payload.startDailyChallenge==`function`?payload.startDailyChallenge:(async()=>{}),startNewGame=typeof payload.startNewGame==`function`?payload.startNewGame:(()=>{}),logError=typeof payload.logError==`function`?payload.logError:(()=>{}),alertFn=typeof payload.alertFn==`function`?payload.alertFn:(()=>{}),setQuestionContainerDisplay=typeof payload.setQuestionContainerDisplay==`function`?payload.setQuestionContainerDisplay:(()=>{}),setWelcomeDisplay=typeof payload.setWelcomeDisplay==`function`?payload.setWelcomeDisplay:(()=>{}),renderWelcomeDailyCard=typeof payload.renderWelcomeDailyCard==`function`?payload.renderWelcomeDailyCard:(()=>{}),count=3;numberEl&&(numberEl.textContent=count),overlayEl&&(overlayEl.style.display=`flex`);let interval=setInterval(()=>{if(--count,count<=0){clearInterval(interval),overlayEl&&(overlayEl.style.display=`none`),mode===dailyMode?startDailyChallenge({preserveBonusStreak}).catch(error=>{let message=error&&error.message?error.message:String(error);logError(`Failed to start daily challenge`,message),alertFn(/already been completed/i.test(message)?`Today's Daily Challenge is already completed. Come back tomorrow.`:`Could not load today's challenge. Please try again.`),setQuestionContainerDisplay(`none`),setWelcomeDisplay(`block`),renderWelcomeDailyCard()}):startNewGame({mode:normalMode,preserveBonusStreak});return}numberEl&&(numberEl.textContent=count,numberEl.style.animation=`none`,numberEl.offsetWidth,numberEl.style.animation=``)},1e3);return!0}function buildCategoryCount(input){let payload=input&&typeof input==`object`?input:{},questions=Array.isArray(payload.questions)?payload.questions:[],counts={"Old Testament":0,"New Testament":0,General:0};return questions.forEach(question=>{question&&Object.prototype.hasOwnProperty.call(counts,question.category)&&(counts[question.category]+=1)}),counts}function buildEndGameSummaryHtml(input){let payload=input&&typeof input==`object`?input:{},isDailyGame=payload.isDailyGame===!0,correctCount=Number.isFinite(payload.correctCount)?payload.correctCount:0,totalQuestions=Number.isFinite(payload.totalQuestions)?payload.totalQuestions:10,percent=Number.isFinite(payload.percent)?payload.percent:0,totalScore=Number.isFinite(payload.totalScore)?payload.totalScore:0,verseText=String(payload.verseText||``),statsHtml=String(payload.statsHtml||``),showRestartSameButton=payload.showRestartSameButton===!0,carryoverBannerHtml=String(payload.carryoverBannerHtml||``),summaryPlayerLevel=Number.isFinite(payload.summaryPlayerLevel)?payload.summaryPlayerLevel:1,playerProfile=payload.playerProfile&&typeof payload.playerProfile==`object`?payload.playerProfile:{name:`Player`,avatarId:``},builtInAvatars=Array.isArray(payload.builtInAvatars)?payload.builtInAvatars:[],getAvatarById=typeof payload.getAvatarById==`function`?payload.getAvatarById:(()=>builtInAvatars[0]||{type:`emoji`,value:``,label:`Avatar`}),escapeHtml=typeof payload.escapeHtml==`function`?payload.escapeHtml:(value=>String(value||``)),fallbackAvatarId=builtInAvatars[0]&&builtInAvatars[0].id?builtInAvatars[0].id:``,avatar=getAvatarById(playerProfile.avatarId||fallbackAvatarId),avatarHtml=avatar&&avatar.type===`image`?`<img src="${escapeHtml(avatar.value)}" alt="${escapeHtml(playerProfile.name||`Player`)} avatar"/>`:`${escapeHtml(avatar&&avatar.value||``)}`;return`
        <h2>${isDailyGame?`Daily Challenge Summary`:`Game Summary`}</h2>
        <p>You got <strong>${correctCount}</strong> right out of ${totalQuestions} (${percent}%).</p>
        <p><strong>Total Score: ${totalScore} points</strong></p>
        <p id="encouraging-verse">${verseText}</p>
        ${statsHtml}
        <div class="summary-player-wrap">
          <button id="summary-player-btn"
            type="button"
            aria-label="Open player profile">
            ${avatarHtml}
          </button>
          <span id="summary-player-name" class="summary-player-name">${escapeHtml(playerProfile.name||`Player`)}</span>
          <span id="summary-player-level" class="summary-player-level">Level ${summaryPlayerLevel}</span>
        </div>
        ${carryoverBannerHtml}
        <div style="margin-top: 30px;">
          ${showRestartSameButton?`<button id="restart-same">&#9654; Play</button>`:``}
          <button id="restart-change">&#x1F3E0; Home</button>
        </div>
      `}globalScope.BibleTriviaEngine=Object.assign({},globalScope.BibleTriviaEngine,{getTimeLimitForMode,calculatePointBreakdown,selectBalancedQuestionsForMode,startCountdownThenGame,buildCategoryCount,buildEndGameSummaryHtml})})(window);