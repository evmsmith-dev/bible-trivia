# Copilot Instructions for This Repository

- When analyzing files, skip all git-related files and folders unless explicitly requested.
- Git-related paths include: `.git/`, `.github/`, `.gitignore`, `.gitattributes`, `.gitmodules`.
- Do not review changed files, diffs, or git status (for example `get_changed_files`, `git diff`, `git status`) unless explicitly requested.
